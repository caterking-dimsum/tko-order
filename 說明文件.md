# 豪隍點心工房（將軍澳）外賣落單網站 — 說明文件

> 最後更新：2026-08-18

一頁式手機外賣落單網站。客人選點心 → 送出前看訂單預覽 → 一鍵把訂單傳到餐廳 WhatsApp。
純靜態網頁、無後端、免費寄存在 GitHub Pages。

---

## 1. 基本資料

| 項目 | 內容 |
|------|------|
| 公開網址 | https://caterking-dimsum.github.io/tko-order/ |
| GitHub repo | https://github.com/caterking-dimsum/tko-order |
| 本地資料夾 | `C:/Users/Admin/Documents/Claude/豪隍外賣落單/` |
| WhatsApp 落單號碼 | `852 9527 0753`（程式內變數 `WA = "85295270753"`） |
| 致電餐廳電話 | `9527 0753`（程式內變數 `TEL = "95270753"`） |
| 頂部招牌文字 | 🥟 豪隍點心工房（將軍澳）外賣 |
| QR code | `qrcode.png` ⚠️ **仍指向舊網址** `bensiuuuuu23.github.io/caterkingtko-order`（舊站暫時仍可開，但一旦舊 repo 關閉／刪除，已印出的 QR 就會失效，需重新生成） |

檔案結構：`index.html`（整個網站＝版面＋程式＋菜單資料都在這一個檔）、`img/item01..item60`（點心相片，已壓到 900px）、`qrcode.png`、`README.md`、`說明文件.md`（本檔）。

---

## 2. 菜單資料來源（Google Sheets）

- 試算表 ID：`1CjlP7y3EGOFpY5OPuGNYt1XkbUqlfvCGBQx5ET2D2E4`
- 分頁 **工作表1**（餐牌，目前 60 項）：
  - A=編號　B=相片檔名　C=種類　D=**價位**（皇/中/大/頂/特/超/小，或直接寫 $價錢）　E=正價　F=**特價**（茶市價）
- 分頁 **日子**（日曆，399 天）：
  - A=日期(日/月/年)　B=星期　C=**平日/假期**（用來判斷茶市）
- 相片來源資料夾：`~/Downloads/Photo 22-12-2025 豪隍點心工房 /`（飲品暫無相片，用佔位圖）

---

## 3. 計價規則（自動）

1. 每款價錢看 **D 欄價位**：
   - 價位對應：小 $29、中 $31、大 $35、頂 $37、特 $41、超 $43、皇 $57
   - 若 D 欄直接寫數字（例如飲品 $14、煎蛋 $7），就用該數字
2. **茶市特價**：若當天是「平日」（依日子分頁），且香港時間在
   **早上 8:00–10:30** 或 **下午 2:00–5:00**，自動改用 **F 欄特價**。
   非此時段或假期 → 用正價。
3. 每款最多可選 **10 件**。

---

## 4. 茶市時段會顯示在 3 個地方（只有真的在茶市時段才出現）

1. 頂部黃色橫條：🍵 現為茶市時段，部分點心為特價
2. 訂單預覽備注：• 現為茶市時段，已套用特價
3. WhatsApp 訊息：（茶市特價時段）

---

## 5. 主要功能

- 左側種類欄，點一下跳到該分類；捲動時自動高亮目前分類
- 每款點心有相片、名稱（名稱後括號顯示價位，如「豪隍蝦餃皇（中）」）、數量加減
- **選取點心時不顯示價錢**；價錢只在「訂單預覽」出現
- 按「送出訂單」先彈出**訂單預覽**：分類小計 + 合計件數金額 + 3 條備注，確認後才送 WhatsApp
- 送出的 WhatsApp 訊息：列出品項與數量（不含逐項價錢、不含價位）、合計、3 條備注
- 3 條備注：
  1. 下單後將會收到確認，如 5 分鐘內未收到，請致電餐廳 9527 0753
  2. 價錢只為參考，確實價錢以餐廳計算為準
  3. 下單確認後 20 至 25 分鐘可取餐

---

## 6. 怎麼更新

| 想改什麼 | 怎麼做 |
|----------|--------|
| 改某張相片 | 換掉 `img/` 對應的檔，`git commit` + `git push`，約 1 分鐘生效 |
| 改菜單/價錢/分類 | 改 Google Sheet → 請 Claude 重新產生 → push（會重建整個菜單） |
| 改電話/WhatsApp 號碼 | 改 `index.html` 的 `WA` / `TEL` |
| 改招牌、文字、備注 | 改 `index.html` 對應文字 |
| 部署 | `git add` → `git commit` → `git push origin main`，GitHub Pages 約 30–60 秒（偶爾數分鐘）生效 |

> 內容、相片都用 git 版本控制，改壞了可以回退到之前任何一次 commit。

---

## 7. 變更歷史

- 2026-06-11　建站：45 款點心、分類選單、WhatsApp 落單、上線 + QR
- 2026-06-14　修正手機捲動與分類跳轉
- 2026-06-15　大改版：60 項最新資料、價錢、茶市特價、訂單預覽、招牌加「將軍澳／外賣」、網址改 caterkingtko-order、選取時不顯示價錢、預覽欄位對齊
- 2026-06-16　修正「拉到底分類跳不回」、WhatsApp 訊息精簡（不顯示逐項價錢與價位）、6 款改用原相片、電話改 9527 0753、WhatsApp 訊息加入 3 條備注
- 2026-08-18　豪隍素翅餃由小點改為大點（正價 $29 → $35，茶市特價同步 $35）；repo 遷至 `caterking-dimsum/tko-order`，公開網址改為 `caterking-dimsum.github.io/tko-order`

（完整逐次紀錄見 `git log`。）

---

## 8. 待辦 / 已知限制

- 4 款飲品（可樂、無糖可樂、冷泡烏龍、冷泡茉莉）目前是**佔位圖**，有照片可換上。
- 想用 AI **重新生成**點心圖，需先開通 Google 帳單（生圖免費額度為 0）；開好後可用現有 Gemini key 進行。
